---
title: PostHogEditorial violations
---

Tests for PostHogBase + PostHogEditorial rules. Includes violations from both packages.

## Base rule violations

We need to optimise the colour of the page. For details, [click here](https://posthog.com). PostHog — great tool.

## Enable Not Allow

PostHog allows you to track events across your app. This feature allows teams to monitor performance in real time. The SDK allows users to send custom data.

## Hedging

When it comes to analytics tools, it depends. Is PostHog the best? Well, it's complicated. There's no right answer when choosing between tools.
