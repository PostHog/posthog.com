---
title: SCIM
sidebar: Docs
showTitle: true
availability:
    free: none
    selfServe: none
    enterprise: full
    features:
        scim:
            free: false
            selfServe: false
            boost: false
            scale: false
            enterprise: true
---

<FeatureAvailability availability={_frontmatter.availability.features.scim} />

System for Cross-domain Identity Management (SCIM) enables you to automatically provision and deprovision users in PostHog from your Identity Provider (IdP). This is helpful for organizations that need centralized user management, including automatically creating accounts, assigning roles, and disabling accounts when users leave the organization.

## Prerequisites

1. If you are using PostHog Cloud, your organization must be subscribed to a platform package that offers SCIM. If you are self-hosting PostHog, your instance must have an enterprise license that supports SCIM.

2. You need to [verify domains](/docs/settings/authentication/sso#authentication-domains) for the email addresses you want to provision. For example, if you want to provision users with an `@example.com` email address, you need to add and verify `example.com` as an authentication domain.

3. If you are self-hosting PostHog, make sure you have properly configured the `SITE_URL` [environment variable](/docs/self-host/configure/environment-variables).

4. If you are self-hosting PostHog, your PostHog instance must be accessible from the public internet over TLS.

5. Your IdP must support SCIM 2.0. For example, Okta, Microsoft Entra ID, and OneLogin support SCIM 2.0.

## How SCIM works

- Assigning a user to the PostHog application in your IdP creates the user in PostHog with the mapped role.
- Removing a user from the application deactivates their PostHog account.
- Users with matching email addresses in PostHog are updated to match the details and roles from your IdP.
- Roles are matched by name and are case-sensitive.
- SCIM configuration changes, including enabling, disabling, and token rotation, are recorded in the [activity log](/docs/settings/activity-logs).

## Configuring SCIM

1. Navigate to your organization's [authentication settings](https://app.posthog.com/settings/organization-authentication#setting=scim-configuration) page in PostHog.

2. Scroll down to the **SCIM** section, and select **Configure**.

3. Select **Enable SCIM provisioning**, then select **Save configuration**.

4. In your IdP, create a new SCIM app for PostHog.
<CalloutBox icon="IconInfo" title="Note" type="fyi">
If you already have a SAML app in your IdP, and that app supports SCIM 2.0 provisioning, you can use that existing app.
</CalloutBox>

5. Copy the "*SCIM base URL*" and "*Bearer token*" displayed in PostHog into your SCIM app's configuration in your IdP.

6. Configure the role mapping in your IdP. Map your IdP roles to PostHog roles.

7. Assign users and roles to the PostHog application in your IdP.

### Example: Okta

1. In Okta admin, go to **Applications** and select your existing SAML application for PostHog. [Create a SAML application if you do not have one](/docs/settings/authentication/saml#example-okta).
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_17_okta_saml_step_1_3c4abb3562.png"
    alt="Okta admin UI showing listed applications"
    classes="rounded"
   />

2. In the Okta app, under the **General** tab, in **App settings**, select **Edit**.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_settings_bc2bda3579.png"
    alt="Okta admin UI showing application details"
    classes="rounded"
   />

3. Under the **Provisioning** section, select **SCIM**, then select **Save**.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_settings_edit_ece6c4c38d.png"
    alt="Okta admin UI showing application settings form"
    classes="rounded"
   />

4. Navigate to the **Provisioning** tab, then select **Edit**.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_24d472d9fa.png"
    alt="Okta admin UI showing application provisioning tab"
    classes="rounded"
   />

5. Navigate to your PostHog organization's [authentication settings](https://app.posthog.com/settings/organization-authentication#setting=scim-configuration), scroll down to **SCIM**, then select **Configure**.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_17_okta_saml_step_4_light_5657a92830.png"
    imageDark="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_17_okta_saml_step_4_dark_f67925b245.png"
    alt="PostHog UI showing authentication configurations"
    classes="rounded"
   />

6. Select **Enable SCIM provisioning**, then select **Save**.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_posthog_config_saved_light_71aa37242e.png"
    imageDark="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_posthog_config_saved_dark_f9c1080e39.png"
    alt="PostHog UI showing SCIM form"
    classes="rounded"
   />

7. Copy the "*SCIM base URL*" from PostHog into the Okta app's "*SCIM connector base URL*" field.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_base_url_73a5be4ae4.png"
    alt="Okta SCIM form with SCIM base URL completed"
    classes="rounded"
   />

8. In the Okta app, change "*Authentication Mode*" to "*HTTP Header*".
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_auth_mode_b1a486a77d.png"
    alt="Okta SCIM form with Authentication Mode completed"
    classes="rounded"
   />

9. Copy the "*Bearer token*" from PostHog into the Okta app's "*Authorization*" field.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_auth_token_1b10f001dd.png"
    alt="Okta SCIM form with Authentication completed"
    classes="rounded"
   />

10. In the Okta app, under "*Unique identifier field for users*" enter a field that uniquely identifies your users. Most likely this will be `email`.
   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_unique_id_8830ffbd48.png"
    alt="Okta SCIM form with Unique Identifier completed"
    classes="rounded"
   />

11. Under **Supported provisioning actions** select the following options:
   - Push New Users
   - Push Profile Updates
   - Push Groups

   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_provisioning_actions_a69c18c89f.png"
    alt="Okta SCIM form with Support provisioning actions completed"
    classes="rounded"
   />

12. Select **Test Connector Configuration**. Verify that Okta can communicate with PostHog.

13. Navigate to the **Assignments** tab and assign the application to users and groups.
   - Click **Assign to People** and select the users you want to provision into PostHog.
   - Click **Assign to Groups** and select the Okta groups to provision into PostHog.

   <ProductScreenshot
    imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/q_auto,f_auto/2026_09_24_okta_scim_application_assignments_c4d73edba1.png"
    alt="Okta application assignments tab"
    classes="rounded"
   />

14. Go to the **Push Groups** tab.
   - To push specific groups, select **Find groups by name**, then choose **Create Group** (creates a new role in PostHog) or **Link Group** (links it to an existing PostHog role). When you link the group to an existing role in PostHog, the role name will be updated to match Okta.
   - If you have lots of groups, you can choose **Find groups by rule**. This way, all groups matching the rule are pushed to PostHog.
   - Verify the pushed groups show as **Active**.

Users and roles will now be automatically provisioned in PostHog by Okta. For additional detailed Okta instructions, see [Okta's SCIM provisioning documentation](https://help.okta.com/en-us/content/topics/apps/apps_app_integration_wizard_scim.htm).

### Example: OneLogin

If you already have a SAML app configured, you'll need to migrate to an app that supports SCIM provisioning.

1. In OneLogin admin, go to **Applications** and click **Add App**.

2. Search for `SCIM Provisioner with SAML (SCIM v2 Enterprise, full SAML)` and select it.

3. In the **Configuration** tab, copy the settings from your existing SAML app: SAML Audience URL, RelayState, ACS (Consumer) URL.

4. Go to the **Parameters** tab and ensure the following attributes are created (they are required for SAML to work correctly):
   - `email` (Email)
   - `first_name` (First Name)
   - `last_name` (Last Name)

5. Navigate to the **Configuration** tab and paste the **SCIM Base URL** and **SCIM Bearer Token** from PostHog into the API Connection section. Click **Enable**.

6. Go to the **Provisioning** tab and check **Enable provisioning**. Select whether you want admin approval for creating or deleting users.

7. To sync roles, go to the **Rules** tab and create a rule to map OneLogin roles to PostHog roles. To sync all roles as-is:
   - In **Actions**, select `Set Groups in SCIM v2 Provisioner with SAML`
   - Select `Map from OneLogin`
   - For each `role` with value that matches `.*`

8. Go to the **SSO** tab and copy the new **Issuer URL**, **SAML 2.0 Endpoint**, and **X.509 Certificate**. Update your existing SAML configuration in PostHog with these new values.

9. Assign users to the new application. Go to **Users**, select a user, assign roles, and add them to the application.
