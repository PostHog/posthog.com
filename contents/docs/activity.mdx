---
title: Activity tab
sidebar: Docs
showTitle: true
---

The activity tab is a customizable list of events that you've captured. It's great for checking events are being captured, debugging captured events, and being a jumping off point for further analysis. You can expand each event to its specific properties, metadata, raw JSON, and feature flags.

<ProductScreenshot
  imageLight="https://res.cloudinary.com/dmukukwp6/image/upload/Clean_Shot_2025_07_16_at_13_25_44_2x_c459debf3b.png"
  imageDark="https://res.cloudinary.com/dmukukwp6/image/upload/Clean_Shot_2025_07_16_at_13_26_23_2x_f1d2038057.png"
  alt="Activity tab"
  classes="rounded"
/>

The options for customization on the activity tab include:

1. Filtering by date, event, properties, and more.

2. Customizing the columns shown. These can include event properties, person properties, feature flags, and even SQL queries. You can reorder them and set your customization as the default for all project members.

For example, to show an absolute timestamp like `Jul 16, 09:12:55`, you can add an SQL expression like `formatDateTime(toTimeZone($timestamp, 'America/Chicago' ), '%b %d, %H:%i:%s')`.

3. Toggling between relative and absolute time for date-time columns. Click the column header menu (three dots) on a date-time column like **Time** and select **Show absolute time** to display the full date and time instead of relative time like "2 minutes ago." This toggle is also available on the **Events** tab of person profiles.

## Activity views

The activity tab also has multiple views:

1. **PostHog default:** Shows a list of events with their person, URL or screen, library, and time.

2. **Project default:** Shows a list of events with the configured columns you set.

3. **Event count:** Shows each event along with the number of times it's been captured.

4. **Live:** Shows a live feed of events as they are captured as well as the number of currently active users. This can be filtered for a specific event or paused.

5. **Jump to timestamp:** Press `J` then `T` to quickly navigate to events around a specific point in time.

## Terminal live events

You can view live PostHog events directly in your terminal using the PostHog CLI.

### Installation

Install the CLI via npm:

```bash
npm install -g @posthog/cli
```

### Usage

Authenticate and start the livestream:

```bash
posthog-cli login
posthog-cli livestream
```

By default, the command opens an interactive TUI (terminal user interface). The TUI shows a scrollable list of events with real-time stats like events per minute and total count.

Filter events using CLI flags:

```bash
posthog-cli livestream --event-type "$pageview" --distinct-id "user_123"
```

For JSON output (useful for piping to other tools), use the `--json` flag:

```bash
posthog-cli livestream --json
```

JSON mode is also used automatically when piping output or using the `--geo` flag.

### Keyboard shortcuts

| Key         | Action                    |
| ----------- | ------------------------- |
| `j` / `↓`   | Move cursor down          |
| `k` / `↑`   | Move cursor up            |
| `Enter`     | View event details        |
| `p`         | Pause/resume event stream |
| `f`         | Filter by event type      |
| `d`         | Filter by distinct ID     |
| `x`         | Clear all events          |
| `?`         | Toggle help               |
| `Esc`       | Close detail/filter view  |
| `q`         | Quit                      |

### Legacy tool (posthog-live)

A standalone `posthog-live` binary is also available for download from the [GitHub releases page](https://github.com/PostHog/posthog/releases?q=posthog-live&expanded=true). The CLI version above is recommended for most users.
