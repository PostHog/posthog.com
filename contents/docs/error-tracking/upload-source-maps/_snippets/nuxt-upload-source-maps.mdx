import { Steps, Step } from 'components/Docs/Steps'
import InstallCLI from './nuxt/install-cli.mdx'
import NuxtOutputSourceMaps from './nuxt/output-source-maps.mdx'
import NuxtInjectSourceMap from './nuxt/inject-source-map.mdx'
import NuxtUploadSourceMap from './nuxt/upload-source-map.mdx'

<Steps>
    <Step title="Install the PostHog CLI" subtitle="Your goal in this step: Install the PostHog CLI tool." badge="required">
        <InstallCLI />
    </Step>
    <Step title="Output source maps for your framework" subtitle="Your goal in this step: Configure Nuxt to generate source maps during build." badge="required">
        <NuxtOutputSourceMaps />
    </Step>
    <Step title="Inject source map" subtitle="Your goal in this step: Inject metadata into source maps to associate them with your code." badge="required">
        <NuxtInjectSourceMap />
    </Step>
    <Step title="Upload source map" subtitle="Your goal in this step: Upload the injected source maps to PostHog during build." badge="required">
        <NuxtUploadSourceMap />
    </Step>
</Steps>
