{/* This file is intentionally left as a stub. Product Tours has been removed from the site, but a cached MDX scope from earlier builds still references this path. The stub keeps the cache resolvable until it naturally rotates. */}
