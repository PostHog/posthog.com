import CommandHelmUpgradeSnippet from './command-helm-upgrade'

To upgrade the chart using [Helm >= v3](https://helm.sh/) with the release name `posthog` in `posthog` namespace, run the following:

<CommandHelmUpgradeSnippet />

> Note: before upgrading, please take a look at the [upgrade notes](/docs/self-host/deploy/upgrade-notes) and the
Helm [documentation](https://helm.sh/docs/helm/helm_upgrade/).
