> **Note:** The following self-hosted proxy isn't provided by PostHog, so we can't take responsibility for it! If unsure, we recommend using [our managed reverse proxy](/docs/advanced/proxy/managed-reverse-proxy).
