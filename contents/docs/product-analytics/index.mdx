---
title: Product analytics
sidebar: Docs
showTitle: true
contentMaxWidthClass: max-w-5xl
---

import { IconLaptop, IconPlug, IconCode, IconBrackets, IconBolt, IconSend, IconPerson, IconProfile, IconGraph, IconNotebook, IconPullRequest, IconCheckCircle } from '@posthog/icons'
import OSButton from 'components/OSButton'
import CustomSelfDrivingLoop from 'components/CustomSelfDrivingLoop'

Product analytics answers what people actually do in your product. Build trends, funnels, retention, paths, stickiness, and lifecycle insights on the events you already send to PostHog, then save them to dashboards, share them, and get alerted when they move.

Because it runs on the same events, persons, and properties as the rest of PostHog, every number you look at is one click away from the [session replay](/docs/session-replay) behind it, the [feature flag](/docs/feature-flags) that gated it, or the [experiment](/docs/experiments) that changed it. Those same insights feed [Self-driving](/docs/self-driving), which watches your saved insights for regressions and opens pull requests you review and merge.

<OSButton variant="primary" asLink to="/docs/product-analytics/start-here">Get started</OSButton>

## Where you can use it

You build and explore insights in the PostHog web app. The other surfaces let you query the same data, act on it, and pull it into your own tools.

<div className="not-prose grid grid-cols-1 @md:grid-cols-2 gap-x-6 gap-y-6 my-6">

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconLaptop className="size-6 shrink-0 text-blue mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">Web app</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Build insights, assemble dashboards, share them, and set alerts on the numbers that matter.</p>
      <a href="/docs/product-analytics/surfaces/web-app" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Build insights →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconPlug className="size-6 shrink-0 text-purple mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">MCP</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Run trends, funnels, retention, and SQL queries from any MCP client or AI editor.</p>
      <a href="/docs/product-analytics/surfaces/mcp" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Query over MCP →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconCode className="size-6 shrink-0 text-orange mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary flex items-center gap-1.5">PostHog Desktop <span className="text-2xs font-bold uppercase tracking-wide px-1.5 py-0.5 rounded bg-yellow/20 text-orange">Beta</span></p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Review Self-driving reports about metric regressions and merge the fixes they open.</p>
      <a href="/docs/product-analytics/surfaces/desktop" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Fix regressions in PostHog Desktop →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconBrackets className="size-6 shrink-0 text-seagreen mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">API</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Create and read insights, and run any analytics query, from your own systems.</p>
      <a href="/docs/product-analytics/surfaces/api" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Use the API →</a>
    </div>
  </div>

</div>

## Where its data comes from

Product analytics runs on the events your product sends to PostHog and the persons and properties attached to them. Get those right and every insight downstream gets easier.

<div className="not-prose grid grid-cols-1 @md:grid-cols-2 gap-x-6 gap-y-6 my-6">

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconSend className="size-6 shrink-0 text-orange mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">Events</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Custom events you capture from any SDK, each with the properties you attach to them.</p>
      <a href="/docs/product-analytics/capture-events" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Capture events →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconBolt className="size-6 shrink-0 text-red mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">Autocapture</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Pageviews, clicks, and form submissions captured for you, so you have data before you instrument.</p>
      <a href="/docs/product-analytics/autocapture" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Set up autocapture →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconPerson className="size-6 shrink-0 text-purple mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">Persons and identity</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Link anonymous and logged-in activity to one person so funnels and retention count real users.</p>
      <a href="/docs/product-analytics/identify" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Identify users →</a>
    </div>
  </div>

  <div className="flex items-start gap-3 rounded border border-primary p-5 bg-primary">
    <IconProfile className="size-6 shrink-0 text-seagreen mt-0.5" />
    <div>
      <p className="m-0 font-bold text-primary">Person and group properties</p>
      <p className="m-0 mt-0.5 text-sm text-secondary">Plan, role, company, and anything else you set – the dimensions you break every insight down by.</p>
      <a href="/docs/product-analytics/person-properties" className="inline-flex items-center gap-1 mt-1.5 text-sm font-semibold">Set person properties →</a>
    </div>
  </div>

</div>

## How product analytics works with Self-driving

Your product analytics data is a [signal source](/docs/self-driving/inbox/sources) for [Self-driving](/docs/self-driving). A scout watches your saved funnel, retention, lifecycle, stickiness, and paths insights for rate regressions – a conversion step that started dropping, retention that flattened – and turns what it finds into a report. When there's a code fix, that report hands off to a coding agent that opens a pull request.

<CustomSelfDrivingLoop
  stages={[
    { label: 'Insights', icon: IconGraph, color: '#F54E00', description: 'Your saved funnels, retention, and lifecycle insights' },
    { label: 'Signals', icon: IconBolt, color: '#FFA81C', description: 'Rate regressions surfaced by a scout' },
    { label: 'One report', icon: IconNotebook, color: '#29DBBB', description: 'Grouped with the replays and events behind the drop' },
    { label: 'Draft PR', icon: IconPullRequest, color: '#A737D2', description: 'When a code fix is possible' },
    { label: 'You merge', icon: IconCheckCircle, color: '#47C861', description: 'Nothing ships without a human' },
  ]}
/>

You build and explore insights in the [PostHog web app](/docs/product-analytics/surfaces/web-app), and review and merge the Self-driving work in the [Self-driving inbox](/docs/self-driving/inbox). See the [Self-driving docs](/docs/self-driving) for the full picture.
