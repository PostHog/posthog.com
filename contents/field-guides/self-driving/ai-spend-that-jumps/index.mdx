---
title: AI spend that jumps overnight
subtitle: 'Did your AI feature get more expensive without more usage?'
badge: beta
filters:
  type:
    - self-driving
  maintainer: official
premise: >-
  An LLM bill moves for one of two reasons: more conversations, or more cost per conversation. The
  first is growth. The second is a regression, and it compounds – a prompt grew, a model changed, a
  cache prefix stopped hitting, or a retry looped.
tldr: >-
  This scout watches your LLM traces and makes your AI features and agents faster, cheaper, and
  more reliable.
watches:
  - name: AI Observability
    detail: Cost, tokens, model, cache reads, and error flags on every generation – enough to spot the change and attribute it.
  - name: Your codebase
    detail: Deploys, so a cost change can be tied to the commit that caused it rather than just a timestamp.
requires:
  - label: AI Observability instrumented, so $ai_generation events are arriving
    level: required
  - label: A property on generations identifying the feature, so you can split cost per surface
    level: required
  - label: Your codebase connected via GitHub, to correlate a change against deploys
    level: recommended
category: AI Observability
schedule: Daily
report:
  title: Cost per conversation up 3.1x while volume is flat
  source: Scout · AI cost
  receivedAgo: 4h
  affected: '~$230/day above the 14-day norm'
  body: >-
    Since yesterday's 09:14 deploy, conversations are flat at roughly 1,200/day but cost per
    conversation rose from $0.09 to $0.28. Generations per conversation went from 1.2 to 3.6 while
    input tokens and model distribution held steady. The extra generations carry non-zero cost and
    a `max_tokens` stop reason rather than a 429, so the provider billed for every retry – this is
    a retry loop, not a bigger prompt or a cache miss.
  suggestedAction: >-
    Cap the retry count on the generation path and surface the underlying failure instead of
    retrying silently.
  actionNote: An agent opens the pull request – you review and merge.
---
