---
# A starter for one use case in a pocket guide. Copy this whole directory to
# contents/pocket-guides/<volume>/<your-slug>/ and replace every value below.
#
# A use case is two files:
#   index.mdx  – this file: structured data in the frontmatter, every word in the body
#   SKILL.md   – the scout itself, a real file you could paste into the monorepo
#
# Directories starting with `_` never become pages, so this one is safe to leave here.
# Authoring guide: src/components/PocketGuides/README.md

title: Starter scout template
shortTitle: Starter
# Not shown on the page – it's the meta description for search and link previews.
subtitle: 'One line on what this use case is, for search results.'

# Reading order in the book. 0 is the front matter; every other page is numbered in
# sequence. Leave this out entirely and the page exists but stays out of the book –
# which is how you keep a draft unlisted.
# bookOrder: 5

filters:
  type:
    - self-driving

# What the scout reads. Rendered as <Watches /> in the body.
watches:
  - name: A PostHog product
    detail: What it reads from this source, and why that beats a single metric.

# What someone needs before this scout can run. Levels: required, recommended, optional.
requires:
  - label: The thing that must be instrumented
    level: required

# The product surface this belongs to. Drives grouping wherever guides are listed.
schedule: Daily

# The report the scout would file. This is the most important thing you write –
# see components/SelfDrivingInbox/README.md for the full field contract.
report:
  title: The finding as a claim with its evidence in it, not a topic
  source: Scout · what surfaced it
  receivedAgo: 2h
  affected: The impact, e.g. 47 users affected
  body: >-
    2–4 sentences. Lead with the observation, then the corroborating evidence from
    other sources. Markdown works: `code`, **bold**, and links.
  suggestedAction: >-
    What the agent proposes doing about it.
  actionNote: An agent opens the pull request – you review and merge.
---

{/*The left page. Figures pull from the frontmatter above – no wiring needed.*/}

<LeftPage>

<ReportFigure
    n={1}
    caption="Your inbox, reading this report."
    legend="1 – Which scout wrote it, and when. 2 – What the agent proposes doing about it. 3 – Most runs write nothing; a report is the exception."
/>

<ScoutFigure n={2} caption="The scout – the file an agent follows on a schedule." />

</LeftPage>

{/*The right page. Plain markdown – write it the way you'd write a manual.*/}

<RightPage>

# Starter scout template

Two or three sentences on why this is worth watching, for someone who has never heard of scouts.
Set up the problem only.

The takeaway on its own line. Always starts "This scout…" and says what you get, not what it
inspects.

## The report in your inbox

A <Term name="scout" /> files a <Term name="report" /> when it finds this – Fig. 1 is the one this
use case is about.

## The pull request you merge

What the agent changes, and what it costs.

## The scout itself

What this scout watches, and – the part worth writing carefully – what would make it stay quiet.

<Watches />

## The schedule it runs on

How often it runs, and why most runs write nothing.

## Add this to your scout troop

<!-- The requirement checklist comes from `requires:` in this file's frontmatter;
    the scout itself (name, description, what the button prefills) is ../SKILL.md. -->
<Enable />

</RightPage>
