---
# SHELL – every line of the body below is copied verbatim from the internal handbook, in its
# original sales/CS voice. Nothing here has been rewritten for the book yet.
#
# Sources:
#   handbook/growth/use-case-selling/customer-experience.md:21,28,47,57,153-158,197
title: From ticket to replay
shortTitle: Ticket to replay
subtitle: The ticket tells you what they said. The replay shows what happened.
bookOrder: 7
---

<RightPage>


<div className="not-prose my-6 rounded border-4 border-red bg-red/10 p-6 text-center text-xl font-bold text-red">THIS IS PLACEHOLDER GENERATED BY MR.CLYDE</div>

# From ticket to replay

## The ticket arrives with the evidence

Because PostHog already captured what happened in the product, each ticket arrives with the sender's session replay, recent events, and exceptions attached.

See exactly what the user did, not what they think they did. Capture console logs and network calls alongside the visual recording. Widget tickets attach the customer's session automatically, so replay is one click from the conversation.

## "We can't reproduce bugs"

Support needs to see what happened instead of relying on screenshots and user descriptions. Session Replay is the direct answer, whether or not tickets move.

## Why this is the connection that matters

The ticket tells them what the customer *said*. Replay shows what actually happened.

## What it looks like when it's working

- Every conversation lands in one inbox, with the customer's replay, events, and errors already attached
- Support shares one link (replay + errors + logs) and engineering has full context in seconds
- Engineers see replay + errors + logs without switching tools or asking "can you try that again?"
- Support resolves technical tickets without escalating, because the evidence is already in the ticket

## The claim underneath it

The conversation and the evidence in one place. Every other helpdesk is a text box with a CRM attached. Support ships with the customer's session replay, events, and exceptions on the ticket, because the same platform captured them. No integration to configure, no sampling gaps, no "can you send a screenshot?"

</RightPage>
