## Q4 2023 Goals

1. Build the Web Analytics Prototype
  - Build a prototype that we can use internally.
  - Release to a few customers using feature flags.
  - Start working towards the architecture to scale to a general release.