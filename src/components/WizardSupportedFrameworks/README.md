# WizardSupportedFrameworks

Renders the wizard “Frameworks and languages” grid for docs. Data comes from `getWizardFrameworkRows()` in `src/constants/installation-taxonomy.ts` (same taxonomy as `Installation.tsx`).
