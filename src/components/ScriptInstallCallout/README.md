# ScriptInstallCallout

Copyable install command block (title, description, optional footer). Uses `CopyableCommand` with the wizard gradient by default. Footer often wires in `WizardFrameworksTeaser` for product “Get started” sections.
