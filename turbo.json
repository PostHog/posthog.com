{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": ["public/**", ".cache/**", ".vercel/**"],
            "env": ["NODE_ENV"]
        }
    }
}
